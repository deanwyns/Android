<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="hogent.hogentprojecteniii_groep10.VacationOverview">

    <include
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        layout="@layout/toolbar"
        android:id="@+id/include" />

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vacation_overview_list_view"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/naam_btn" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/vacation_name_sort_btn"
        android:id="@+id/naam_btn"
        android:layout_below="@+id/include"
        android:layout_alignParentStart="true" />

    <Button
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/vacation_date_sort_btn"
        android:id="@+id/datum_btn"
        android:layout_toEndOf="@+id/naam_btn"
        android:layout_below="@+id/include" />

</RelativeLayout>
